# To do:

- See if a better way to do what i am doing with the ResourceWrapper to pass information about if a resource was created
- give user_exercises a public key (uuid? name with an autogenerated default value?)
- Do request validation the spring way
    - global (controller advice) and controller handler method error handlers
        - test for problem detail shape
- Add other endpoints
    - test moving exercises to other exercise types, disallow this for user workouts
    - test changing children for coupled 1:many relationships (like lists of workout statistics)
- Implement security
