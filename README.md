# To do:

- Repository interface
- Generic JOOQ record mapper/unmapper? (low priority tbh)
- Do request validation the spring way (whatever that is xD)
- Generic CrudService implementation (depends how req validaion works)
- Way to better generate test data
    - Should be clear there are only two needed entities, and should only need to provide the changes to them
    - decouple db testing from response fields
